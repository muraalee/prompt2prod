# Firebase Auto-Provisioning Backend Configuration

# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# GCP Service Account Credentials
# Option 1: Base64-encoded JSON key (recommended for Cloud Run)
# GCP_SERVICE_ACCOUNT_KEY=base64_encoded_service_account_json

# Option 2: JSON string (for local development)
# GCP_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# GCP Organization/Folder (optional - where new projects will be created)
# GCP_ORGANIZATION_ID=123456789
# GCP_FOLDER_ID=987654321

# Firestore Configuration
FIRESTORE_LOCATION=us-central1

# To get your service account:
# 1. Go to Google Cloud Console
# 2. Navigate to IAM & Admin > Service Accounts
# 3. Create a new service account with these roles:
#    - Firebase Admin
#    - Project Creator
#    - Service Usage Admin
#    - Cloud Resource Manager API Admin
# 4. Create a JSON key and base64 encode it:
#    cat service-account-key.json | base64 > service-account-key.base64
# 5. Copy the contents to GCP_SERVICE_ACCOUNT_KEY
